# AGENTS.md

## Project context
This workspace is a small Java practice project. The main source file is currently `ReverseArray.java`, and the code is expected to stay simple and beginner-friendly.

## Working conventions
- Prefer plain Java without extra frameworks, libraries, or build tools unless the task explicitly requires them.
- Keep edits focused on the current file unless a task clearly requires a new file.
- Preserve the class name and filename relationship for Java source files.
- Favor readable, straightforward logic over clever or over-engineered implementations.
- When validating changes, compile from the project root with:
  - `javac ReverseArray.java`

## Verification
- If the user asks for implementation changes, validate by compiling the Java file before considering the task complete.
- If there are no tests in the project, compilation is the expected proof of correctness.

## Scope guardrails
- Do not introduce unrelated project structure or configuration.
- Do not add dependency management or build tools for a simple single-file exercise.
- If the task is to reverse an array, keep the solution aligned with basic Java arrays and loops.
